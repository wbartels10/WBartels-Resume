# Resume Website

A simple static resume website scaffolded for `WBartels`. It includes:

- `index.html` — resume content and page structure
- `styles.css` — responsive visual layout and theme
- `script.js` — small interactive hover effect

## Usage

Open `index.html` in a browser to view the site.
